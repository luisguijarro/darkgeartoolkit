using System;

namespace dgtk
{	
	public enum HatPosition // Usamos los valores de Windows para no marear la perdiz.
	{
		Center = 8, Up = 0, Down = 4, Left = 6, Right = 2, UpLeft = 7, UpRight = 1, DownLeft = 5, DownRight = 3
	}
}